
<div class="row">
  <div class="col-md-6 col-md-offset-3">
		<%= form_tag("/search", method: "post") do %>
        <%= label_tag(:brand, "Buscar: ")%>
          <%= text_field_tag(:brand) %>
		  <%= submit_tag("Search", class: 'btn btn-info', id:'js-search') %>
		<% end %>

    <hr>

    <!--<form action="/search" method="get">-->
      <!--<input type="text" name="get[name]" id="brand" class="js-search">-->
      <!--<button class="btn btn-info" type="submit">Search</button>-->
    <!--</form>-->


        <table class="table table-striped table-hover ">
          <thead>
          <tr>
            <th>Brand</th>
          </tr>
          </thead>
          <% @brand.each do |item|%>
          <tbody>
          <tr class="active">
            <td><%= link_to item.name %></td>
          </tr>
          </tbody>
        </table>

  <% end %>

  </div>
</div>
